# config.py

import os
from dataclasses import dataclass, field

@dataclass
class Config:
    BOT_TOKEN: str = os.getenv("BOT_TOKEN")
    
    # Multiple Groq API keys для rotation
    GROQ_API_KEYS: list = field(default_factory=lambda: os.getenv("GROQ_API_KEYS", "").split(","))
    
    # Supabase
    SUPABASE_URL: str = os.getenv("SUPABASE_URL")
    SUPABASE_KEY: str = os.getenv("SUPABASE_KEY")
    
    # Admin IDs для статистики
    ADMIN_IDS: list = field(default_factory=lambda: [
        int(x) for x in os.getenv("ADMIN_IDS", "").split(",") if x.strip()
    ])
```

## Что изменилось:

### ❌ Убрано:
- Система банов
- Таблица violations
- Счетчик предупреждений (warnings)
- Жесткая модерация

### ✅ Добавлено:
- **Мягкая проверка контента** - бот вежливо просит отправить учебные материалы
- **Benefit of doubt** - при ошибке распознавания бот пропускает изображение
- **Лог вопросов** для статистики (не для наказания)
- **Вежливые ответы** на любой контент
- Команда `/help`
- Показ распознанного текста перед обработкой

### Примеры вежливых ответов:
```
Неучебное фото:
"😊 Я вижу это изображение, но не могу найти здесь учебное задание. 
Отправьте фото страницы учебника или тетради, и я помогу разобраться!"

Нечеткое фото:
"😊 Изображение нечёткое. Попробуйте сфотографировать ещё раз при хорошем освещении."

Ошибка обработки:
"😔 Извините, произошла временная ошибка.

Попробуйте:
- Переформулировать вопрос
- Подождать минуту
- Написать вопрос покороче"
